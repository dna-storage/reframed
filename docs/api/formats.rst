Format Registry (``dnastorage.system.formats``)
================================================

Maps format names (strings used in config files and CLI arguments) to their
encoder/decoder builder functions and metadata. Format IDs embedded in DNA
file headers are permanent — once assigned they must never be reused.

.. automodule:: dnastorage.system.formats
   :members:
   :undoc-members:
